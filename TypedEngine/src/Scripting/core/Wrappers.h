#pragma once

// #include "core/transform.h"

// class Sprite;

class TESprite {
public:
  TESprite();

  void init();
  // TESprite(Transform transform);
  // TESprite(const char* texture);
  // TESprite(Transform transform, const char* texture);

  // void setTransform(Transform transform);
  // Transform getTransform();

// private:
//   Sprite* ref;
};

// class TEText {
// public:
//   TEText();
// };

// class TEQuad {
// public:
//   TEQuad();
// };
